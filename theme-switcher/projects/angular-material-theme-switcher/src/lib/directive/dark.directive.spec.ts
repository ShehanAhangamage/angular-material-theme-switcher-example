import { DarkDirective } from './dark.directive';

describe('DarkDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkDirective();
    expect(directive).toBeTruthy();
  });
});
